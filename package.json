{
  "name": "projeto-clientes",
  "version": "1.0.0",
  "description": "Projeto de Vistoria - CRUD com JSON Server",
  "scripts": {
    "server": "json-server --watch db/db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
